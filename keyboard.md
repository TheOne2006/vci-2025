# 控制器控制与可调节参数

本文档列出了 `controller.cpp` 中的控制和可调节参数。

## 控制 (手柄)

该应用程序设计为通过手柄进行控制。

如果使用键盘控制 (WASD)，默认键位映射如下：

| 输入 | 动作 | 描述 |
| :--- | :--- | :--- |
| **W / A / S / D** | 移动 | 控制角色的移动方向和速度。 |
| **方向键 (↑ / ↓ / ← / →)** | 相机 / 朝向 | 控制相机轨道。侧移时，它还控制角色的朝向。 |
| **Space (空格)** | 侧移 | 按住以启用侧移模式。 |
| **Q** | 拉近 | 拉近相机。 |
| **E** | 拉远 | 拉远相机。 |
| **Left Shift (左 Shift)** | 行走 | 按住以强制角色行走而不是奔跑。 |

## 可调节参数 (GUI)

该应用程序具有图形用户界面 (GUI)，允许您实时调整各种模拟和动画参数。

### 模拟对象 (Simulation Object)
控制“模拟”对象（橙色胶囊/骨架）的运动特性，角色会尝试跟随该对象。
*   **velocity halflife (速度半衰期)**：控制模拟速度对输入的响应程度。数值越低，移动越灵敏。 (默认值: 0.27)
*   **rotation halflife (旋转半衰期)**：控制模拟旋转对输入的响应程度。 (默认值: 0.27)
*   **run forward speed (奔跑前进速度)**：向前奔跑时的最大速度。 (默认值: 4.0)
*   **run sideways speed (奔跑侧向速度)**：向侧面奔跑时的最大速度。 (默认值: 3.0)
*   **run backwards speed (奔跑后退速度)**：向后奔跑时的最大速度。 (默认值: 2.5)
*   **walk forward speed (行走前进速度)**：向前行走时的最大速度。 (默认值: 1.75)
*   **walk sideways speed (行走侧向速度)**：向侧面行走时的最大速度。 (默认值: 1.5)
*   **walk backwards speed (行走后退速度)**：向后行走时的最大速度。 (默认值: 1.25)

### 惯性化混合 (Inertialization Blending)
*   **halflife (半衰期)**：控制使用惯性化进行动画过渡的平滑度。数值越高，过渡越平滑但越慢。 (默认值: 0.1)

### 反向运动学 (Inverse Kinematics / IK)
控制脚部放置和锁定。
*   **enabled (启用)**：切换 IK。 (默认值: 开启)
*   **blending halflife (混合半衰期)**：IK 混合的平滑度。 (默认值: 0.1)
*   **unlock radius (解锁半径)**：脚部从地面解锁的距离。 (默认值: 0.2)